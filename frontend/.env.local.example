# ============================================================
# AI Study Companion — Frontend Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================

# Base URL of the FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================
# SECURITY REMINDER
# ============================================================
# Never add API keys, database credentials, or secrets here.
# All sensitive keys live ONLY in the backend .env file.
# This file is ONLY for public, non-sensitive config.
